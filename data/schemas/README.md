# Data schemas

`v1/artifacts.json` is the authoritative machine-readable column contract for generated Parquet
artifacts. `v1/manifest.schema.json` documents the publication manifest. Schema versions change only
for contract changes; data versions identify regenerated datasets under the same schema.
