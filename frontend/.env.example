# Backend origin used by the browser during direct local development.
VITE_API_BASE_URL=http://localhost:8000
# Label for the immutable artifact version selected by the backend.
VITE_ARTIFACT_DATA_VERSION=test-v1
